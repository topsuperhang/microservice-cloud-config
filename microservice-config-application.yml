#注意：如果在记事本上编写，小面的缩进不要使用Tab来缩进，不然会报错
spring:
  profiles:
    active: dev #激活开发环境
---
server:
  port: 4001 #开发环境
spring:
  profiles: dev
  application:
    name: microservice-config-dev
---
server:
  port: 4002 #端口号
spring:
  profiles: prod #生产环境
  application:
    name: microservice-config-prod

#保存时注意，选择UTF-8保存